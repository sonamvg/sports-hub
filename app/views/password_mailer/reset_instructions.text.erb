We received a request to reset the password for your PodiumCircle account (<%= @user.email %>).

Reset your password here (expires in 15 minutes):
<%= @reset_url %>

If you didn't request this, you can safely ignore this email.
