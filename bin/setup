#!/usr/bin/env ruby
require "fileutils"
APP_ROOT = File.expand_path("..", __dir__)
Dir.chdir(APP_ROOT) do
  system("bundle check") || system("bundle install") || abort("bundle install failed")
  system("bin/rails db:prepare") || abort("database setup failed")
  puts "PodiumCircle is ready. Run: bin/rails server"
end
